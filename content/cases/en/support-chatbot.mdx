---
slug: support-chatbot
title: E-Commerce Support Bot
description: A customer support chatbot for an online store — built without vector search. Structured knowledge in the system prompt, conversation history in Supabase, prompt caching to cut API costs.
metric: Routine support queries — handled automatically, 24/7
tools: ["n8n", "Claude API", "Supabase", "Prompt Caching"]
icon: MessageSquare
status: active
highlights:
  - { value: "< 2 sec", label: "response to any customer question" }
  - { value: "24/7", label: "without agents or breaks" }
  - { value: "−87%", label: "token cost via prompt caching" }
features:
  - { icon: "MessageSquare", title: "Instant answers", desc: "Responds to questions about products, prices, delivery, payment, and warranty — without a human agent." }
  - { icon: "Database", title: "Conversation memory", desc: "Every exchange is saved to Supabase. The bot remembers the full context of the current session." }
  - { icon: "Zap", title: "Prompt caching", desc: "The system prompt with store knowledge is cached for 1 hour. Subsequent calls read from cache — 87% cheaper." }
  - { icon: "Globe", title: "Responds in the customer's language", desc: "Claude detects the language and responds accordingly — no extra configuration." }
sections:
  - title: "The problem"
    type: text
    body: "~80% of e-commerce support is routine: price, delivery, payment, return policy. A human agent answers the same questions dozens of times a day — during business hours only. Customers write at 11pm and wait until morning. The fix isn't more agents. It's automating what doesn't require a human — with store knowledge in the prompt, not a vector database."

  - title: "How it works"
    type: split
    screenshot: "/cases/support-chatbot/chat-demo.png"
    steps:
      - step: 1
        label: "Customer asks"
        desc: "Any question about products, delivery, payment, or policy"
      - step: 2
        label: "History loaded from Supabase"
        desc: "All previous turns pulled from the `cag` table and formatted into messages array"
      - step: 3
        label: "Claude reads context"
        desc: "System prompt (store knowledge) + history + new question passed to Claude Sonnet"
      - step: 4
        label: "Answer returned in < 2 sec"
        desc: "Accurate, grounded reply — only from what's in the system prompt"
      - step: 5
        label: "Exchange saved"
        desc: "Row written to Supabase: user message + AI response. Memory grows with each turn"

  - title: "The payoff"
    type: cards
    items:
      - icon: Zap
        title: "< 2 sec response time"
        desc: "Instant answers around the clock — no queue, no waiting, no human in the loop for routine queries."
      - icon: Clock
        title: "24/7 without breaks"
        desc: "Works at 11pm, on weekends, on holidays. Coverage that no human team can match at this cost."
      - icon: Sparkles
        title: "−87% token cost"
        desc: "System prompt cached for 1 hour via Anthropic's extended-cache-ttl beta. 87% cheaper per cached call."
      - icon: Shield
        title: "No hallucinations"
        desc: "Claude answers only from what's in the system prompt. Wrong chunk problem doesn't exist — there are no chunks."

  - title: "What's under the hood"
    type: cards
    items:
      - icon: Zap
        title: "n8n — orchestration"
        desc: "Single workflow: chat trigger → load system prompt → load history → format → call Claude → save response."
      - icon: Database
        title: "Supabase — memory"
        desc: "Table `cag` stores every exchange. All rows returned on each call — no external session management needed."
      - icon: Brain
        title: "Claude Sonnet — the brain"
        desc: "Direct API call via HTTP Request node. Full control: model, temperature, caching headers, messages format."
      - icon: Sparkles
        title: "Prompt caching"
        desc: "System prompt cached 1h, last conversation turn cached 5min. Anthropic extended-cache-ttl beta enabled."

  - title: "Open source. Self-hostable."
    type: callout
    body: "The workflow JSON is open — import into your n8n, replace the system prompt with your store's knowledge, connect Supabase. Done."
    cta: "Get the workflow"
---
